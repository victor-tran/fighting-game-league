<div class="like-btn">
  <%= form_for(current_user.liked_posts.find_by(post_id: post.id),
               html: { method: :delete },
               remote: true) do |f| %>
    <%= f.submit "Unlike", class: "btn btn-primary" %>
  <% end %>
</div>