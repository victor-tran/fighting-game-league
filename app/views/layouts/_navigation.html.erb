<!-- User logged in navbar -->
<div class="navbar-wrapper">
  <div class="container">

    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Fighting Game League", root_path, id: "logo" %>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Fighters", users_path %></li>
            <li><%= link_to "FGL Stats", stats_path %></li>
            <li><%= link_to "About", about_path %></li>
            <% if signed_in? %>
              <!-- Notification list -->
              <li class="dropdown notification-icon">
                <a href="#" class="glyphicon glyphicon-exclamation-sign dropdown-toggle" data-toggle="dropdown"><span class="badge unread-notifications">10</span></a>
                <ul class="dropdown-menu scrollable-menu">
                  <%= render partial: 'home/notification_feed' %>
                </ul>
              </li>
              <!-- Settings list -->
              <li class="dropdown">
                <a href="#" class="glyphicon glyphicon-cog dropdown-toggle" data-toggle="dropdown"><b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                  <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to "Sign in", signin_path %></li>
            <% end %> 
          </ul>
        </div>
      </div>
    </div>

  </div>
</div>