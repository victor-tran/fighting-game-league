<% provide(:title, "Set Date") %>

<%= simple_form_for @match, html: { method: :patch },
                            url: set_date_match_path(@match) do |f| %>

  <h1>Set Date</h1>
  <%= f.input :match_date, label: "Match Date and Time" %>
  <%= f.button :submit, class: "btn-lg btn-primary" %>
<% end %>