<div class="like-btn">
  <%= form_for(current_user.liked_posts.build(post_id: post.id),
               remote: true) do |f| %>
    <div><%= f.hidden_field :post_id %></div>
    <%= f.submit "Like", class: "btn btn-primary" %>
  <% end %>
</div>