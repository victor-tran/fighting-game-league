<%= form_for(current_user.memberships.find_by(league_id: @league),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Leave league", class: "btn btn-large btn-warning" %>
<% end %>