<%= form_for(match, html: { method: :patch }, :url => confirm_score_match_path(match.id)) do |f| %>
  <%= f.hidden_field :p1_accepted, :value => true %>
  <%= f.submit "Accept score", class: "btn btn-success", 
                :confirm => "Are you sure?" %>
<% end %>